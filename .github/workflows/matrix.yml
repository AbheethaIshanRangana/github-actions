name: Matrix
on: push

jobs:
  node-version:
    staps:
      - name: Log node version
        run: node -v
      - name: Setup node action 
        uses: actions/setup-node@v1 
        with: 
          node-version: 6
      - name: Log node version 
        run: node -v